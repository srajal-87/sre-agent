"""Prompts for the investigation graph.

Split from the graph itself so a prompt change is a reviewable diff on its own,
and so the wording can be tested (ASCII, cache-stable, no answer key) without
importing a model client.
"""
